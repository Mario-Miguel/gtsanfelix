apiVersion: v1
kind: Service
metadata:
  name: gtsanfelix-backend
  namespace: gtsanfelix
spec:
  selector:
    app: gtsanfelix-backend
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
